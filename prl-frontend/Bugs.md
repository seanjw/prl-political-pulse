Bugs
Searching and then searching something else before the first search completes does not cancel the first search and research properly
The search button should not disable when a previous search is loading; clicking the search button again should research
Clicking the party share bar no longer searches with the selected party (optional feature)
Fields don’t light up when they are changed automatically by default search cards, filters, or the chart (optional feature, but requested)
Badge filters no longer search (but they do change the fields)
You can no longer uncheck the badge filters
Autocomplete no longer works
House-chamber interaction rules no longer apply
No more “Who they are” and “What they’re saying” headers
Sorting dropdown renders behind the first result when “Sorting by Newest” clicked
Chart download button does not work
Loading spinner does not always appear (appears like a silent fail)
Twitter link does not work when “Show More Info” is clicked
Chart panel should not appear if there is no chart data

Chart not always showing (cannot find rule for replication yet)
Typing in search bar changes chart title in real time


Party letters not showing party share bar even with ample room
